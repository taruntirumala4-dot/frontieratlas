export default function Loading() {
  // The MethodDetailClient component handles its own skeleton loading state
  // This loading.tsx just prevents a blank flash during route transition
  return null;
}
